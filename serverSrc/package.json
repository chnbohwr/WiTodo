{
  "name": "serversrc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sls offline start",
    "deploy": "rm -rf node_modules/ && npm install --production && sls deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^1.9.0",
    "serverless-offline": "^3.13.0"
  }
}
